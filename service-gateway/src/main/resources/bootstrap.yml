spring:
  application:
    name: service-gateway
  cloud:
    config:
      username: user1
      password: passwd123   
      uri: http://localhost:8888
    
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false

    
encrypt:
  key: MysecretKey
